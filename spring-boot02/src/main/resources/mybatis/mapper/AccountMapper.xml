<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.test.springboot.dao.AccountDao">

    <!--<select id="loginAccount" resultType="Account">
            select * from t_account where a_username=#{aUsername} and a_password=#{aPassword}
    </select>-->
</mapper>